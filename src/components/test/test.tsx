import type { ReactNode } from "react";

const Test = (): ReactNode => {
    return (
        <section className="bg-blue w-screen h-screen">
            <h1>Hello world!</h1>
        </section>
    );
};

export default Test;
